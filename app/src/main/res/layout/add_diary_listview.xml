<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <ImageView
        android:id="@+id/iv_add_diary_camera"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@mipmap/ic_add_photo"
        android:clickable="true" />

    <TextView
        android:id="@+id/tv_add_diary_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/iv_add_diary_camera"
        android:text="02-20 일기#1"
        android:layout_marginTop="8dp"
        android:textSize="20sp"
        android:layout_marginLeft="16dp"/>

    <ImageView
        android:id="@+id/iv_add_diary_cancel"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginRight="8dp"
        android:layout_marginTop="8dp"
        android:layout_alignParentRight="true"
        android:src="@mipmap/ic_cancel"
        android:visibility="gone"/>

    <EditText
        android:id="@+id/et_add_diary_memo"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_toRightOf="@id/iv_add_diary_camera"
        android:layout_below="@id/tv_add_diary_title"
        android:layout_marginTop="8dp"
        android:layout_marginLeft="16dp"
        android:maxHeight="80dp"
        android:gravity="top"/>

</RelativeLayout>